plugins {
    id "com.android.application"
}

android {
    namespace "dev.core.worker"
    compileSdk 35

    defaultConfig {
        applicationId "dev.core.worker"
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName "0.1.0"
    }
}
