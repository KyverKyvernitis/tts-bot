plugins {
    id "com.android.application"
}

android {
    namespace "dev.core.worker"
    compileSdk 35

    defaultConfig {
        applicationId "dev.core.worker"
        minSdk 26
        targetSdk 35
        versionCode 3
        versionName "0.3.0"
    }
}
