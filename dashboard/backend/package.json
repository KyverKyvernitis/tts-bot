{
  "name": "osaka-dashboard-server",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "test": "node --import tsx --test tests/*.test.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json && node scripts/copy-command-catalog.mjs"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "tsx": "^4.23.11",
    "typescript": "^5.6.2"
  }
}
